"""
Задача1. Создать программно файл в текстовом формате, записать в него построчно данные,
вводимые пользователем. Об окончании ввода данных свидетельствует пустая строка.
Решение:
"""
with open('les5_tsk1.txt', 'w', encoding='utf-8') as f_obj:
    str_ = '_'
    while str_ != '':
        str_ = input('введите данные (выход - пустая строка): ')
        print(str_, file=f_obj)
        # f_obj.write(str_ + '\n')  # через .write()
