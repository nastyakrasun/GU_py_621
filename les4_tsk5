"""
Задача5. Реализовать формирование списка, используя функцию range() и возможности генератора.
В список должны войти четные числа от 100 до 1000 (включая границы).
Необходимо получить результат вычисления произведения всех элементов списка.
Подсказка: использовать функцию reduce().
Решение:
"""
import functools

print('результат вычисления произведения всех четных чисел от 100 до 1000 (включая границы)')

#def my_func(num1, num2):  # удобнее свернуть в lambda-функцию
#    return num1 * num2

print(functools.reduce(lambda num1, num2: num1 * num2, [ el for el in range(100, 1001) if el % 2 == 0]))
