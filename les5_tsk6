"""
Задача6. Необходимо создать (не программно) текстовый файл, где каждая строка описывает учебный предмет и
наличие лекционных, практических и лабораторных занятий по этому предмету и их количество.
Важно, чтобы для каждого предмета не обязательно были все типы занятий.
Сформировать словарь, содержащий название предмета и общее количество занятий по нему.
Вывести словарь на экран.

Примеры строк файла:
Информатика: 100(л) 50(пр) 20(лаб).
Физика: 30(л) — 10(лаб)
Физкультура: — 30(пр) —

Пример словаря:
{“Информатика”: 170, “Физика”: 40, “Физкультура”: 30}
Решение: les5_tsk6.txt:
Информатика: 100(л) 50(пр) 20(лаб).
Физика: 30(л) 0(пр) 10(лаб)
Физкультура: 0(л) 30(пр) 0(лаб)
"""
with open('les5_tsk6.txt', 'r+', encoding='utf-8') as obj_read:
    lessons = []
    hours = []
    dict_result = {}
    for line in obj_read:
        line = line.split(':')
        lessons.append(line[0])  # пополняется список уроков
        line_1 = line[1].split()
        hours_res = []
        i = 0
        while i < len(line_1):
            numbers = line_1[i].split('(')
            i += 1
            hours_res.append(float(numbers[0]))
        hours.append(sum(hours_res))  # пополняется список часов
    #print(lessons)
    #print(hours)
    j = 0
    while j < len(lessons):
        dict_result[lessons[j]] = hours[j]
        j += 1
    print(dict_result)  # Вывести словарь на экран
    #obj_read.write('\nCловарь, содержащий название предмета и общее количество занятий по нему: \n')
    #obj_read.write(str(dict_result))
